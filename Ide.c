#include <stdio.h>
void main()
{
    int n;
    printf("enter the number");
    scanf("%d",&n);
    if(n>0)
    printf(" no is positive");
    else if(n<0)
    printf("\n the number is negative");
    else 
    printf("the no is zero");

}
